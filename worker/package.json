{
  "name": "pokemon-scraper-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "commands": "node command-setup.js",
    "test": "node --test"
  },
  "dependencies": {
    "discord-interactions": "^4.4.0",
    "mongodb": "^7.1.1"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
